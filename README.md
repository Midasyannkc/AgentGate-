# AgentGate-
Here's the GitHub "About" description for AgentGate (308/350  Zero-trust gateway: Every agent authenticates via mTLS client certs, tool calls are checked against an OPA policy, rate-limited per agent, and logged for audit before reaching the backend. Runs on Kubernetes, provisioned via Terraform, tested and built through a GitHub Actions pipeline. 
